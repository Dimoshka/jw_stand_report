<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.pp.dimoshka.jw_stand_report">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ua.pp.dimoshka.jw_stand_report.preferences"
            android:label="@string/settings"
            android:theme="@style/Theme.AppCompat.Light"/>

        <activity
            android:name="ua.pp.dimoshka.jw_stand_report.preferences_old"
            android:label="@string/settings"
            android:theme="@style/Theme.AppCompat.Light"/>

        <activity
            android:name="ua.pp.dimoshka.jw_stand_report.preferences_new"
            android:label="@string/settings"
            android:theme="@style/Theme.AppCompat.Light"/>

    </application>

</manifest>
